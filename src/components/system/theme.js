import { createTheme } from '@mui/material/styles';

const theme = {
  palette: {
    primary: {
      main: '#333'
    },
    secondary: {
      main: '#E33E7F'
    }
  }
}

export default createTheme(theme)
